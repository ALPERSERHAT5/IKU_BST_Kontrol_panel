{
  "name": "bst-kontrol-paneli-backend",
  "version": "1.0.0",
  "description": "BST Kontrol Paneli - Kurum içi teknik destek sistemi",
  "main": "src/server.js",
  "type": "module",
"scripts": {
  "dev": "node --watch src/server.js",
  "start": "npx prisma migrate deploy && node src/server.js",
  "postinstall": "prisma generate",
  "prisma:generate": "prisma generate",
  "prisma:migrate": "prisma migrate dev",
  "prisma:seed": "node prisma/seed.js"
},
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.1",
    "express-rate-limit": "^7.4.1",
    "express-validator": "^7.2.0",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "node-cron": "^3.0.3",
    "qrcode": "^1.5.4"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
